<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:id="@+id/main"
    tools:context=".ConfiguracionActivity">

    <!-- Barra de la aplicación con título y fondo rojo -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="Configuración"
            app:titleTextColor="@android:color/white"
            android:background="#C62828" />
    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/app_bar_layout"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp"
            android:paddingBottom="16dp">

            <!-- ========= SECCIÓN GENERAL ========= -->
            <TextView
                style="@style/SettingsCategoryTitle"
                android:text="@string/general" />

            <!-- Opción Idioma (Clickeable) -->
            <LinearLayout
                android:id="@+id/option_language"
                style="@style/SettingsItem"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">
                <ImageView style="@style/SettingsItemIcon" android:src="@drawable/ic_languaje"
                    android:contentDescription="@string/icono_de_idioma" />
                <LinearLayout style="@style/SettingsItemTextContainer">
                    <TextView style="@style/SettingsItemTitle" android:text="@string/idioma" />
                    <TextView
                        android:id="@+id/tv_current_language"
                        style="@style/SettingsItemSubtitle"
                        android:text="@string/espa_ol" />
                </LinearLayout>
                <ImageView style="@style/SettingsItemArrow" />
            </LinearLayout>

            <!-- Opción Cerrar Sesión -->
            <LinearLayout
                android:id="@+id/option_logout"
                style="@style/SettingsItem"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">
                <ImageView style="@style/SettingsItemIcon"
                    app:tint="#D32F2F"
                    android:contentDescription="@string/icono_de_cerrar_sesi_n" />
                <LinearLayout style="@style/SettingsItemTextContainer">
                    <TextView
                        style="@style/SettingsItemTitle"
                        android:text="@string/cerrar_sesi_n"
                        android:textColor="#D32F2F"/>
                </LinearLayout>
            </LinearLayout>

            <View style="@style/SettingsDivider" />

            <!-- ========= SECCIÓN ACERCA DE ========= -->
            <TextView
                style="@style/SettingsCategoryTitle"
                android:text="@string/acerca_de1" />

            <!-- Opción Versión de la app -->
            <LinearLayout style="@style/SettingsItem">
                <ImageView style="@style/SettingsItemIcon" android:src="@drawable/ic_info"
                    android:contentDescription="@string/icono_de_informaci_n" />
                <LinearLayout style="@style/SettingsItemTextContainer">
                    <TextView style="@style/SettingsItemTitle" android:text="@string/versi_n_de_la_app" />
                    <TextView style="@style/SettingsItemSubtitle" android:text="@string/_1_0_0" />
                </LinearLayout>
            </LinearLayout>

            <!-- Opción Términos y Política de Privacidad -->
            <LinearLayout
                android:id="@+id/option_privacy"
                style="@style/SettingsItem"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground">
                <ImageView style="@style/SettingsItemIcon" android:src="@drawable/ic_policy"
                    android:contentDescription="@string/icono_de_pol_tica_de_privacidad"/>
                <LinearLayout style="@style/SettingsItemTextContainer">
                    <TextView style="@style/SettingsItemTitle" android:text="@string/t_rminos_y_pol_tica_de_privacidad" />
                </LinearLayout>
                <ImageView style="@style/SettingsItemArrow" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
